package scene;

import eleeye.Position;
import io.github.humbleui.skija.Canvas;
import ui.Sprite;

public class Depction {
//    private void drawPieces(Canvas canvas) {
//
//        int pc = -1;
//        for (int i = 0; i < 255; i++) {
//            if (Position.IN_BOARD(i) && (pc =  position.squares[i]) > 0) {
//                drawPiece(canvas, sprite_pieces[pc], i, 0, 0);
//            }
//        }
//
//    }
//
//    private void drawPiece(Canvas canvas, Sprite piece, int sq, int i2, int i3) {
//        if (piece == null) return;
//        int x = ((Position.FILE_X(sq) - 3) * 26) + 5 + i2;
//        int y = ((Position.RANK_Y(sq) - 3) * 26) + 21 + i3 + y_t;
//        if (Position.DST(mv_current) != sq || mv_current  <= 0) {
//            piece.draw(canvas);
//            return;
//        }
//
//        int x_src = ((Position.FILE_X(Position.SRC(mv_current)) - 3) * 26) + 5;
//        int y_src = ((Position.RANK_Y(Position.SRC(mv_current)) - 3) * 26) + 21 + this.y_t;
//        int x_dst = ((Position.FILE_X(Position.DST(mv_current)) - 3) * 26) + 5;
//        int y_dst = ((Position.RANK_Y(Position.DST(mv_current)) - 3) * 26) + 21 + this.y_t;
//
//        int i5 = 26;
//        int i6 = 26;
//        if (Math.abs(y_dst - y_src) == Math.abs((x_dst - x_src) << 1)) {
//            i5 = 13;
//        }
//        if (Math.abs(y_dst - y) == Math.abs((x_dst - x_src) >> 1)) {
//            i6 = 13;
//        }
////        int abs = x_dst - x_src != 0 ? x_src + (br * i5 * (((x_dst - x_src) / i5) / Math.abs((x_dst - x_src) / i5))) : x_src;
////        int abs2 = y_dst - y != 0 ? y + (br * i6 * (((y_dst - y) / i6) / Math.abs((y_dst - y) / i6))) : y;
//
//
//
//    }


}


